/* eslint-disable @typescript-eslint/no-var-requires */
// import config from './config';

// // const fs = require('fs');

// // app.listen(config.listenPort, () => {
// //   console.log(`Server Start in ${config.listenPort} port!`);
// // });
import './src/index';
