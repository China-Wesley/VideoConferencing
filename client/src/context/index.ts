import React from 'react';

export const User = React.createContext({} as any);

export const Room = React.createContext({} as any);

export const RTCStream = React.createContext({} as any);

export default {
  User,
  Room,
  RTCStream,
};
